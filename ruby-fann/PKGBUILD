# Generated by gem2arch
# Contributor: crab <crabtw@gmail.com>

_gemname=ruby-fann
pkgname=$_gemname
pkgver=1.1.3
pkgrel=1
pkgdesc="Bindings to use FANN from within ruby/rails environment."
arch=('i686' 'x86_64')
url="http://ruby-fann.rubyforge.org"
license=('MIT')
depends=('ruby' 'fann')
makedepends=('rubygems')
source=(http://gems.rubyforge.org/gems/$_gemname-$pkgver.gem)
noextract=($_gemname-$pkgver.gem)
md5sums=('661d8ff93e473d43855d10b3d24715f0')

build() {
  cd $srcdir
  local _gemdir="$(ruby -e'puts Gem.default_dir')"
  gem install --ignore-dependencies -i "$pkgdir$_gemdir" $_gemname-$pkgver.gem
}
