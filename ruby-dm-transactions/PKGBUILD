# Generated by gem2arch
# Contributor: crab <crabtw@gmail.com>

_gemname=dm-transactions
pkgname=ruby-$_gemname
pkgver=1.0.0
pkgrel=1
pkgdesc="Adds transaction support to datamapper"
arch=('i686' 'x86_64')
url="http://github.com/datamapper/dm-transactions"
license=('MIT')
depends=('ruby' 'ruby-dm-core>=1.0.0')
makedepends=('rubygems')
source=(http://gems.rubyforge.org/gems/$_gemname-$pkgver.gem)
noextract=($_gemname-$pkgver.gem)
md5sums=('e0e0f5e47adc6815ef82012285f1dc6e')

build() {
  cd $srcdir
  local _gemdir="$(ruby -e'puts Gem.default_dir')"
  gem install --ignore-dependencies -i "$pkgdir$_gemdir" $_gemname-$pkgver.gem
}
