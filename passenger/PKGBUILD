# Generated by gem2arch
# Contributor: crab <crabtw@gmail.com>

_gemname=passenger
pkgname=$_gemname
pkgver=3.0.9
pkgrel=1
pkgdesc="Easy and robust Ruby web application deployment"
arch=('i686' 'x86_64')
url="http://www.modrails.com/"
license=('MIT')
depends=('ruby' 'rake' 'ruby-fastthread>=1.0.1' 'ruby-daemon_controller>=0.2.5' 'rack>=0')
makedepends=('rubygems')
source=(http://rubygems.org/downloads/$_gemname-$pkgver.gem)
noextract=($_gemname-$pkgver.gem)
md5sums=('d616c8425071303b983b6c09fea8004a')

build() {
  cd $srcdir
  local _gemdir="$(ruby -e'puts Gem.default_dir')"
  gem install --ignore-dependencies -i "$pkgdir$_gemdir" $_gemname-$pkgver.gem
}
